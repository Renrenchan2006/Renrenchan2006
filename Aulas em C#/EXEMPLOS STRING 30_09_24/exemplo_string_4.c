#include<stdio.h>
#include<locale.h>
#include<string.h>
void main (void)
{
  int i;
    char vet[]="Ate achava que aqui batia um coracao";
  i=strlen(vet);
  printf("\nO tamanho do vetor vet eh %d", strlen(vet));
  		
  	system("pause");
  	return 0;
}